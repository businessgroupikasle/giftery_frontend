/**
 * localStorage / sessionStorage key names used across the app.
 */
export const STORAGE_KEYS = Object.freeze({
  TOKEN: 'auth_token',
  USER: 'auth_user',
  CART: 'cart_data',
  THEME: 'app_theme',
  WISHLIST: 'wishlist_ids',
  RECENT_PRODUCTS: 'recent_products',
  CHECKOUT_ADDRESS: 'checkout_address',
});
